# Database & Mail config (contains credentials - NEVER commit this)
connect.php
google_config.php
includes/mail_config.php

# Upload files (user-generated content, too large for git)
uploads/

# Debug / temp files
scratch/
debug.txt
strip_inserts.py
*.log
*.tmp

# SQL dump files (Include ONLY clean starter database for GitHub, exclude live database dumps)
*.sql
!databases/bookwagon_db_clean.sql

# OS files
.DS_Store
Thumbs.db

# IDE files
.vscode/
.idea/
